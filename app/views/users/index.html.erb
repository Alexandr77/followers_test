<div class="container-fluid">
  <h1 class="text-center">Welcome, <%= current_user.name %></h1>
  <h4 class= "text-center"> Choose users to follow </h4>
  <% users.each do |user| %>
    <div class="row">
      <div class="col col-sm"><%= user.name %></div>
      <div class="col col-sm"><%= user.group.name %></div>
      <%= render partial: 'changeable', locals: { user: user } %>
    </div>
  <% end %>
<div>
